[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-chat-client"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["openai"]

[project.scripts]
chat = "chat:main"

[tool.setuptools]
py-modules = ["chat"]
