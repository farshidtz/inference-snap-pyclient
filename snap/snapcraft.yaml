name: pyclient
base: core24
version: '0.1'
summary: Single-line elevator pitch for your amazing snap
description: |
  This is my-snap's description.

grade: stable
confinement: strict

plug:
  socket-provider:
    interface: content
    default-provider: deepseek-r1
    target: $SNAP_DATA/share/connection.json

parts:
  src:
    plugin: nil
